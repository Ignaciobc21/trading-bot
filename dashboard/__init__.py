"""
dashboard/ — Streamlit app para monitorizar el bot en vivo y revisar
backtests guardados.

Se invoca con `python main.py --mode dashboard` (que a su vez ejecuta
`streamlit run dashboard/app.py`). El app no se importa directamente
desde código; vive como entry point standalone.
"""
